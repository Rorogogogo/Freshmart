'use client'
import React from 'react'
import { usePathname } from 'next/navigation'

// This component is now empty since search is integrated in the navbar
const GlobalSearch: React.FC = () => {
  return null
}

export default GlobalSearch
